# Everything needed to run the full pipeline: downloads, modelling, spatial stats, tests.
# Use this locally:  pip install -r requirements-dev.txt

-r requirements.txt

# This project's own code (src/yieldpred), installed in editable mode so that
# `import yieldpred` works from anywhere - scripts, notebooks, pytest. The app
# deliberately does not rely on this; see the note in requirements.txt.
-e .

# Data download
requests
python-dotenv

# Modelling
scikit-learn

# Spatial statistics (Moran's I and the contiguity weights it needs)
esda
libpysal

# Raster work (Cropland Data Layer step, planned)
rasterio

# Testing
pytest
