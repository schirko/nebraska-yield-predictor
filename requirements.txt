# Core data
pandas>=2.2
numpy
pyarrow
requests
python-dotenv

# Modeling
scikit-learn

# GIS
geopandas
rasterio
matplotlib

# Spatial statistics (Moran's I and the weights it needs)
esda
libpysal

# App
streamlit

# Testing
pytest

# This project's own code (src/yieldpred), installed in editable mode
-e .
